'use strict';

const pi = 3.14;

const sumValues = (x,y) => x + y;
const subtractValues = (x,y) => x - y;
const calculateCircleArea = (r) => r ** 2 * pi;

export {
    sumValues,
    subtractValues,
    calculateCircleArea
};